#include "monty.h"

/**
  * main - Entry point.
  *
  * Return: Always EXIT_SUCCESS.
  */

int main(int argc, char **argv)
{
	if (argc != 2)
	{
		printf("USAGE: %s\n", argv[0]);
		return (EXIT_FAILURE);
	}
}
